<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kevin.provider">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.kevin.provider.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.kevin.provider.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">
        <service
            android:name="com.kevin.provider.helper.widget.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <receiver android:name="com.kevin.provider.helper.widget.ImageFavoriteWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/image_favorite_widget_info" />
        </receiver>

        <provider
            android:name="com.kevin.provider.helper.provider.MyContentProvider"
            android:authorities="com.kevin.provider"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.kevin.provider.READ_DATABASE"
            android:writePermission="com.kevin.provider.WRITE_DATABASE"
            tools:replace="exported" />

        <receiver
            android:name="com.kevin.provider.helper.alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="@string/todo"
            tools:ignore="ManifestResource" />

        <activity android:name="com.kevin.provider.view.setting.SetReminderActivity" />
        <activity android:name="com.kevin.provider.view.favorite.FavoriteActivity" />
        <activity android:name="com.kevin.provider.view.detail.DetailActivity" />
        <activity
            android:name="com.kevin.provider.view.search.SearchActivity"
            android:launchMode="singleTask" />
        <activity android:name="com.kevin.provider.view.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>