<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.kevin.provider">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.kevin.provider.READ_DATABASE"
        android:protectionLevel="normal" />
    <permission
        android:name="com.kevin.provider.WRITE_DATABASE"
        android:protectionLevel="normal" />

    <application
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">

        <service
            android:name=".helper.widget.StackWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <receiver android:name=".helper.widget.ImageFavoriteWidget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/image_favorite_widget_info" />
        </receiver>

        <provider
            android:name=".helper.provider.MyContentProvider"
            android:authorities="com.kevin.provider"
            android:enabled="true"
            android:exported="true"
            android:readPermission="com.kevin.provider.READ_DATABASE"
            android:writePermission="com.kevin.provider.WRITE_DATABASE"
            tools:replace="exported" />

        <receiver
            android:name=".helper.alarm.AlarmReceiver"
            android:enabled="true"
            android:exported="false" />

        <activity android:name=".view.setting.SettingsActivity" />
        <activity android:name=".view.favorite.FavoriteActivity" />
        <activity android:name=".view.detail.DetailActivity" />
        <activity
            android:name=".view.search.SearchActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>